
# Flask Environment
FLASK_ENV=production

# Secret Key
SECRET_KEY=dev-secret-key-change-in-production

# Database - PostgreSQL
DATABASE_URL=postgresql://rollcall_user:muk1234muk@localhost:5432/rollcall_db

# PostgreSQL Connection Pool Settings
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=50
DB_POOL_RECYCLE=300
DB_POOL_TIMEOUT=30

# Email Settings (for password reset)
MAIL_SERVER=smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USE_TLS=True
MAIL_USE_SSL=False
MAIL_USERNAME=your-mailtrap-username
MAIL_PASSWORD=your-mailtrap-password
MAIL_DEFAULT_SENDER=noreply@rollcall.mak.ac.ug

# Face Recognition
FACE_MATCH_TOLERANCE=0.5

# GPS Settings
GPS_BASE_RADIUS_METRES=40
GPS_MAX_ACCURACY_BUFFER_METRES=25
GPS_REP_ACCURACY_WARN_METRES=35

# Rectify Settings
RECTIFY_TOKEN_TTL_SECONDS=60
RECTIFY_ATTEMPTS_BEFORE_QUEUE=2

# Class Reps
MAX_CLASS_REPS_PER_PROGRAMME=4

# Attendance
ATTENDANCE_THRESHOLD=75

# Flask Debug Mode
FLASK_DEBUG=false